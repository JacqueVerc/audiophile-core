{% extends 'base.html.twig' %}

{% block title %}Audiophile | {{ category.name|capitalize }}{% endblock %}

{% block body %}
    <div>
        <section class="hero-title">
            <h1>{{ category.name|upper }}</h1>
        </section>
        <section class="container">
            <div class="category">
                {% for product in products %}
                {% if loop.index%2 == 0 %}
                <div class="category-article category-article-reverse">
                    {% else %}
                    <div class="category-article">
                        {% endif %}
                        <picture class="category-article-img">
                            <source srcset="{{asset('/images/products/product-' ~ product.slug ~ '/desktop/image-category-page-preview-' ~ product.slug ~ '.jpg')}}" media="(min-width: 1024px)"/>
                            <source srcset="{{asset('/images/products/product-' ~ product.slug ~ '/tablet/image-category-page-preview-' ~ product.slug ~ '.jpg')}}" media="(min-width: 500px)"/>
                            <img src="{{asset('/images/products/product-' ~ product.slug ~ '/mobile/image-category-page-preview-' ~ product.slug ~ '.jpg')}}"  alt={{product.name}} />
                        </picture>
                        <div class="category-article-content">
                            <h2 class="mid-title">{{ product.name|upper }}</h2>
                            <p class="text">
                                {{ product.description }}
                            </p>
                            <a class="btn btn-orange" href="/category/{{ category.name }}/{{ product.slug }}">SEE PRODUCT</a>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </section>
        {% include('layouts/link.html.twig') %}
        {% include('layouts/gearLocation.html.twig') %}
    </div>
{% endblock %}
